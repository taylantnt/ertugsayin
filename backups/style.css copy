* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  height: 100%;
  overflow: hidden;
  font-family: 'Oi', serif;
  font-weight: 400;
  font-style: normal;
  background: #fdfcf7;
  transition: background 0.5s ease;
}

body {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  padding-left: 5rem;
  position: relative;
  background: radial-gradient(circle at center, #fdfcf7 42.44%, #969696);
}

nav {
  display: flex;
  flex-direction: column;
  gap: 7rem;
  text-align: left;
  z-index: 2;
}

.nav-item {
  position: relative; /* Added to contain absolute children */
  font-size: 6vw;
  color: #111;
  cursor: pointer;
  transition: transform 0.5s ease;
  will-change: transform;
}

.nav-text {
  position: absolute;
  top: 0;
  left: 0;
  transition: opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  white-space: nowrap; /* Prevent text wrapping */
  will-change: opacity;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
}

.nav-text.normal {
  font-family: 'Oi', serif;
  font-weight: 400;
  font-style: normal;
  opacity: 1;
}

.nav-text.hover {
  font-family: 'Libre Barcode 39 Text';
  opacity: 0;
}

.nav-item:hover {
  transform: translateX(10px);
}

.nav-item:hover .nav-text.normal {
  opacity: 0;
}

.nav-item:hover .nav-text.hover {
  opacity: 1;
}

.profile-image {
  position: absolute;
  right: 30px;
  bottom: -40px;
  width: 700px;
  max-width: 100%;
  height: auto;
  z-index: 1;
  transition: width 0.5s ease, height 0.5s ease;
}

.page {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background: #111;
  color: white;
  font-size: 2.5rem;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.5s ease;
  padding: 2rem;
  text-align: center;
  z-index: 3;
}

.page.active {
  opacity: 1;
  pointer-events: all;
}

.back-button {
  position: absolute;
  top: 1rem;
  left: 1rem;
  font-size: 1.5rem;
  background: transparent;
  border: 2px solid white;
  color: white;
  padding: 0.5rem 1rem;
  cursor: pointer;
  transition: all 0.3s ease;
  z-index: 4;
}

.back-button:hover {
  background: white;
  color: #111;
}

@media (max-width: 768px) {
  body {
    padding-left: 2rem;
  }

  .nav-item {
    font-size: 3rem;
  }

  .profile-image {
    position: absolute;
    bottom: -20px;
    right: -100px;
    width: 700px;;
    height: auto;
    display: block;
  }
}